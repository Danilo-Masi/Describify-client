export default function ProductCrossPosting({ divStyle }: { divStyle: string }) {
    return (
        <div className={`${divStyle} w-full flex flex-col items-center justify-center gap-y-5 p-5 rounded-lg text-white bg-custom-elevation4 dark:bg-dark-elevation4 border border-custom-borderGray dark:border-dark-borderGray`}>
            Funzioni di crossposting da implementare
        </div>
    );
}
